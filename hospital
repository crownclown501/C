#include <stdio.h>  //falta poner lo demas 
#include <string.h>
#include <conio.h>

struct paciente {
	char nombre[100];
	int numeroPaciente;
	char corre[100];
	int celular;
	char historial[10000];
	int edad;
	char sexo[1];
	int diaConsulta;
	char horaConsulta[5];
	int mesConsulta;
} pacientes[365*12];
struct mes {
		int enero[31][12];
		int febreo[28][12];
		int marzo[31][12];
		int abril[30][12];
		int mayo[31][12];
		int junio[30][12];
		int julio[31][12];
		int agosto[31][12];
		int septiembre[30][12];
		int octubre[31][12];
		int noviembre[30][12];
		int diciembre[31][12];
	}meses;
void cupo(void)
{
	
}
//declarar el tipo de constante  junto con el nombre 
void consulta(void)
{
	for (int i=0,i < j,i++)
	{
		if (c==0)  //esta mal en esta parte falta preguntarle al usuario si es 0 el num sino registrarlo 
		{
			printf("Ingrese numero paciente : ");
			scanf("%d",&paciente[i].numero);
			printf("\nIngrese nombre: ");
			scanf("%s", &paciente[i].nombre);
		}
		else 
		{
			printf("\nIngrese nombre: ");
			scanf("%s", &paciente[i].nombre);
			printf("\nIngrese correo: ");
			scanf("%s", &paciente[i].correo);
			printf("\nIngrese num celular : ");
			scanf("%d", &paciente[i].celular);
			printf("\nIngrese sexo: ");
			scanf("%s", &paciente[i].sexo);
			printf("\nIngrese edad: ");
			scanf("%d", &paciente[i].edad);
		}
	}
}
void darconsulta(void)
{
	int c, f, d, ocupado;
	do
	{
		cupo();
		if (ocupado == 1)
		{
			printf("El turno de [%d][%d][%d] no esta disponible, elija otro dia.\n", f, c,);
}
	}
}

int main (void) {
	mostrarMapa();
	int seleccion;
	printf("Menu:\n1) sacar consulta .\n2) registrarse y sacar consulta \n3) registrarse \n");
	scanf("%d", &seleccion);
	switch (seleccion)
	{
	case 1:
		getchar();
		break;
	case 2:
		getchar();
		break;
	case 3:
		getchar();
		break;

} 
